{% extends '_layout.twig' %}

{% block title %}{{ entry.title }} | Bethesda Ministry{% endblock %}

{% block content %}
    <article>
        <h1>{{ entry.title }}</h1>
        <p><strong>Category:</strong> {{ entry.articleCategory }}</p>
        <p><strong>Published on:</strong> {{ entry.postDate|date('F j, Y') }}</p>
            
            {% if entry.articleImage.one() %}
    <img src="{{ entry.articleImage.one().url }}" alt="{{ entry.title }}">
{% else %}
    <p>No image available</p>
{% endif %}

        

        <div class="article-content">
            {{ entry.articleContent|raw }}
        </div>

        <p><a href="{{ siteUrl('articles') }}">← Back to Articles</a></p>
    </article>
{% endblock %}
